<p id="notice"><%= notice %></p>

<h1>Comidas</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Precio</th>
      <th>Detalle</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comidas.each do |comida| %>
      <tr>
        <td><%= comida.nombre %></td>
        <td><%= comida.precio %></td>
        <td><%= comida.detalle %></td>
        <td><%= comida.categoria %></td>
        <td><%= link_to 'Show', comida %></td>
        <td><%= link_to 'Edit', edit_comida_path(comida) %></td>
        <td><%= link_to 'Destroy', comida, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Comida', new_comida_path %>
