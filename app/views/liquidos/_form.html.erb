<%= form_with(model: liquido, local: true) do |form| %>
  <% if liquido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(liquido.errors.count, "error") %> prohibited this liquido from being saved:</h2>

      <ul>
      <% liquido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :precio %>
    <%= form.number_field :precio %>
  </div>

  <div class="field">
    <%= form.label :detalle %>
    <%= form.text_field :detalle %>
  </div>

  <div class="field">
    <%= form.label :categoria %>
    <%= form.number_field :categoria %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
